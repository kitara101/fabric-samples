apiVersion: v1
kind: Service
metadata:
  name: ${NAME}-1-peer
  labels:
    app: tracelabel
    org: brand-1
    node: peer
    tier: middle
spec:
  type: NodePort
  ports:
    - port: 7051
      name: listen
      nodePort: 30061
    - port: 7053
      name: events
      nodePort: 30063
  selector:
    app: tracelabel
    org: brand-1
    node: peer
    tier: middle


---
apiVersion: apps/v1beta1 # for versions before 1.8.0 use apps/v1beta1
kind: Deployment
metadata:
  name: peer
  labels:
    app: tracelabel
    org: brand-1
    node: peer
    tier: middle
spec:
  selector:
    matchLabels:
      app: tracelabel
      org: brand-1
      node: peer
      tier: middle
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: tracelabel
        org: brand-1
        node: peer
        tier: middle
    spec:
      containers:
      - image: registry.au-syd.bluemix.net/tracelabel/fabric-peer:latest
        name: fabric-peer
        env:
        - name: CORE_VM_ENDPOINT
          value: unix:///host/var/run/docker.sock
        - name: CORE_PEER_ID
          value: peer0.brand1.com
        - name: CORE_LOGGING_PEER
          value: debug
        - name: CORE_CHAINCODE_LOGGING_LEVEL
          value: debug
        - name: CORE_PEER_LOCALMSPID
          value: Brand1MSP
        - name: PEER_GENERAL_LOCALMSPID
          value: Brand1MSP
        - name: CORE_PEER_MSPCONFIGPATH
          value: /etc/hyperledger/msp/peer/
        - name: CORE_PEER_ADDRESS
          value: peer0.brand1.com:7051
        - name: CORE_LEDGER_STATE_STATEDATABASE
          value: CouchDB
        - name: CORE_LEDGER_STATE_COUCHDBCONFIG_COUCHDBADDRESS
          value: brand-1-couchdb:5984
        - name: CORE_LEDGER_STATE_COUCHDBCONFIG_USERNAME
          valueFrom:
                secretKeyRef:
                  name: brand-1-peer-db
                  key: user
        - name: CORE_LEDGER_STATE_COUCHDBCONFIG_PASSWORD
          valueFrom:
                secretKeyRef:
                  name: brand-1-peer-db
                  key: password
        ports:
        - containerPort: 7051
          name: listen
        - containerPort: 7053
          name: evnets
        volumeMounts:
        - name: msp
          mountPath: /etc/hyperledger/msp/peer
          readOnly: true
        - name: users
          mountPath: /etc/hyperledger/msp/users
          readOnly: true
        - name: dockersocket
          mountPath: /host/var/run/docker.sock
      volumes:
      - name: msp
        secret:
          secretName: brand-1-peer-msp
          items:
          - key: sign_cert
            path: signcerts/peer0.brand1.com-cert.pem
          - key: private_key
            path: keystore/private_key
          - key: ca.brand1.com-cert.pem
            path: cacerts/ca.brand1.com-cert.pem
          - key: tlsca.brand1.com-cert.pem
            path: tlscacerts/tlsca.brand1.com-cert.pem
          - key: admin_cert
            path: admincerts/admin_cert
      - name: users
        secret:
          secretName: tracelabel-peer-users
          items:
          - key: ca_cert
            path: msp/cacerts/ca.brand1.com-cert.pem
          - key: admin_cert
            path: msp/admincerts/Admin@brand1.com-cert.pem
      - name: dockersocket
        hostPath:
          path: /var/run/docker.sock
